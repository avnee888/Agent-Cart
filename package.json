{
  "name": "agentcart-backend",
  "version": "1.0.0",
  "description": "Minimal Express server that securely proxies requests from AgentCart to the Anthropic API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
